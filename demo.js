const player = "Drobga";
const club = "Chelsea";
const result = `${player} plays for ${club}`

const add = (a, b) => a + b;
const results = add(5, 6);
results


const players = {
    name: 'Drobga',
    club: 'chelsea'
}

const { name } = players;
name
club

const playes = (player, club) => `${player} play for ${club}`
const result1 = playes("Drobga", "Chalsea");
result1



const vals = {
    x: 2,
    y: 5
}
const sum = ({ x, y }) => x + y;
const total = sum(vals);
total



